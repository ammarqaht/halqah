# Halqah — environment. Set these in CranL, never in the repo.
# Phase 1 (BUILD_PLAN) introduces the database; until then the app has no secrets.
# DATABASE_URL="postgresql://user:pass@host:5432/halqah"
# AUTH_SECRET=""            # jose JWT signing key
# S3_ENDPOINT=""            # CranL object storage
# S3_ACCESS_KEY_ID=""
# S3_SECRET_ACCESS_KEY=""
# S3_BUCKET="halqah"
