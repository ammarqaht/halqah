# نظام حلقة — الحال كما هو اليوم

> **٢١ سبتمبر ٢٠٢٦.** هذا الملف يصف النظام كما هو **مبنيّ وعامل**، لا كما خُطِّط له.
> `SPEC.md` يصف التصميم، و`متطلبات-نظام-حلقة.pdf` يصف ما اتُّفق عليه مع العميل،
> وهذا يصف ما يعمل فعلًا على القاعدة الحيّة — ليقرأه من يكمل العمل بعدنا،
> أو نقرأه نحن بعد شهرين حين ننسى لماذا فعلنا شيئًا بعينه.

---

## ١ · ثلاث بوّابات، ثلاث جلسات

| البوّابة | من يدخلها | اسم الدخول | كلمة المرور | الجلسة |
|---|---|---|---|---|
| **الإدارة** `/admin` | المشرف | رقم هويته | يعيّنها المشرف الذي أنشأه، ثم يغيّرها هو | ٣٠ دقيقة بلا تفاعل |
| **المعلم** `/teacher` | معلم الحلقة | رقم من ٤ أرقام يبدأ من **٢٠٠١** | مؤقّتة يغيّرها أول دخول | ٣٠ يومًا |
| **الطالب** `/student` | الطالب | رقم من ٤ أرقام يبدأ من **١٠٠١** (آخره اليوم ١١٢٠) | رقم هويته | ١٨٠ يومًا |

**الفصل بنيويّ لا شكليّ.** لكل بوّابة كعكة خاصّة و**جمهور مختلف في رمز JWT**، فرمز المشرف
المقدَّم إلى مسار طالب يُرفض على جمهوره، والعكس. وهذا هو الغرض من فصلها: لابتوب مشرف
و١٢٠ جوّال طالب لا يجتمعان على اعتماد واحد.

الجذر `/` يوجّه حسب الكعكة: طالب ← بوابته، مشرف ← بوابته، ولا أحد ← `/student/login`.

**الأرقام اليوم:** ١٢٠ طالبًا · ٧ حلقات · ٧ معلمين · ٤ مشرفين · ٤١٣ اختبارًا ·
١٦٨ خطة · ٣٥٧٢ صفّ منهج · ١٦٩ حركة نقاط.

---

## ٢ · كيف تتحرّك البيانات

```
      ┌──────────────┐
      │ بوابة المعلم │  يسجّل الحضور والتسميع — يكتب مباشرة في القاعدة
      └──────┬───────┘
             │ day_entries · recitation_lines · student_progress · point_txns
             ▼
      ┌──────────────┐
      │ قاعدة واحدة  │  PostgreSQL على CranL
      └──┬────────┬──┘
         │        │
    يقرأ │        │ يقرأ
         ▼        ▼
   ┌─────────┐  ┌──────────┐
   │ المشرف  │  │  الطالب  │
   └─────────┘  └──────────┘
```

**نمطان مختلفان للكتابة، وهذا مقصود:**

- **المعلم والطالب** يكتبان **صفًّا صفًّا** عبر مسارات مخصّصة (`/api/teacher/day`،
  `/api/student/redeem`). كل كتابة معاملة صغيرة مستقلّة.
- **المشرف** يكتب **دفعةً واحدة** عبر `PUT /api/state` — يرسل ما عنده كلّه فيُستبدل.
  هذا مناسب لجهاز يُبلِّغ بما يحمل، وكارثيّ لجهاز لا يحمل شيئًا بعد.

**ولهذا حارسان لا غنى عنهما:**

1. **رفض الحفظ الفارغ (409).** إذا أرسل المتصفّح قائمة فارغة والقاعدة فيها صفوف،
   يُرفض الحفظ. هذا ليس احتياطًا نظريًّا: حفظٌ فارغ محا بيانات حيّة **مرّتين**
   قبل وضعه.
2. **الكتابة على فرق فقط.** الطلاب والحلقات لا يُعاد كتابتهم إلا إذا اختلف صفّ فعلًا.
   كتابتهم بلا شرط وضعت ١٢٠ تحديثًا متتاليًا في المعاملة فماتت بالمهلة.

**وما لا يمسّه حفظ المشرف إطلاقًا:** جداول بوابة المعلم. `day_entries` و`student_progress`
ليست في قائمة المزامنة، فلا يستطيع متصفّح مشرف أن يمحو تسجيل معلم بحفظ عاديّ.
وحركات النقاط الموسومة `ref_type = 'day'` تُستثنى من الحذف لذات السبب.

---

## ٣ · ما يسجّله المعلم — وأين يظهر

### عنده
- **صفحة اليوم** تفتح نفسها؛ لا زرّ بدء ولا إقفال.
- ثلاثة أوضاع: **اليوم** · **فترة لطالب واحد** · **يوم سابق للحلقة كلّها**.
- يقبل أيّ يوم مضى، ويرفض يومًا لم يأتِ بعد.
- التحضير **ثلاث حالات**: حاضر · متأخر · غائب. («غائب بعذر» حُذفت ١٨ سبتمبر بطلب العميل.)
- ثلاثة أسطر لكل مقرّر: م.ك · م.ص · الدرس — لكل سطر: سمّع/لم يسمّع، وعدد أخطاء، وملاحظة.
- الحفظ **لبطاقة الطالب لا لليوم كلّه** — فلا يتصادم جهازان إلا على طالب بعينه.

### عند المشرف
**المتابعة ← الحضور والتسميع** (`/admin/follow-up?list=register`)

كل حلقة بطاقة: حصيلة أسبوعها، وشريط أيامها الخمسة يقول في كل يوم **كم سُجِّل من كم**.
تُفتح الحلقة فيظهر كل طالب بصفّه — علامة لكل عصر، تُضغط فتُعرض أسطره وأخطاؤه ومن سجّلها.
ومن غاب الأسبوع كلّه يُذكر باسمه.

وفي **الرئيسية**: «تقدّم الحلقات — اليوم» يعرض حضور كل حلقة الآن.

### عند الطالب
**«حضوري وتسميعي»** في صفحته الرئيسية — أسبوع من **الأحد إلى الخميس**، بسهمين يرجع بهما
أسابيع. يضغط اليوم فيرى ما سُمِّع وكم خطأ وملاحظة معلمه.

### قاعدة واحدة تحكم الثلاث
> **الصمت ليس غيابًا.**

اليوم الذي لا سجلّ فيه يقول «لم يُسجَّل» ويُعدّ **وحده** — لا حضورًا ولا غيابًا.
فقد لا يكون المعلم حفظ بعد، وقد لا تكون الحلقة انعقدت. وأن يُقال لطالب إنه غاب
بسلطان النظام خطأ يصل أباه قبل أن يصلنا.

---

## ٤ · التقارير

| التقرير | يحتاج | مصدره |
|---|---|---|
| تقرير حلقة المعلّم | حلقة | الحضور والتسميع **من سجلّ المعلم** |
| التقرير الشامل للطالب | طالب | ومعه قسم «الحضور والتسميع» من سجلّ معلمه |
| إحصاءات الجمعية | — | السجلّ كما هو الآن |
| الجاهزون لاختبار الجمعية | حلقة (اختياري) | قاعدة §٤.٨ |
| قائمة نقاط الحلقة | حلقة | دفتر النقاط |
| لوحة الشرف | حلقة (اختياري) | الأرصدة على المدى |
| فرسان الأسبوع | حلقة (اختياري) | **من سجلّ المعلم** — من استوفى كل يوم |
| تسجيل المعلم — أسبوع | حلقة | **من سجلّ المعلم** |
| بيانات فترة | مدّة | ومعها حضور الفترة وتسميعها |
| تقدّم الحلقات — للفترة | — | حسبة الأوجه |
| قائمة تسليم الهدايا | — | الطلبات المعلّقة |
| اختبارات اليوم | — | الحجوزات |

**وكلها تُطبع في ورقة واحدة** — قِيس كل تقرير على مقاس A4 الحقيقي (٧٠٣ × ١٠٣٢ بكسل بعد
هوامش ١٢مم) وصُحِّح ما تجاوزها. ويبقى اثنان يتجاوزانها **بحقّ**: «بيانات فترة» (سرد كل
اختبارات مدّة) و«بطاقات الطلاب» (١٨ بطاقة في الورقة) — وحشرهما يعني إسقاط بيانات.

**وحصيلة الحضور كلها من ملف واحد** `lib/attendance.ts`، فلا تختلف ورقتان.

---

## ٥ · النقاط

**الرصيد ليس رقمًا مخزَّنًا** بل مجموع الحركات:
`نقاط يومية + اختبارات + أكواد − مشتريات − خصم`

| الطريق | القيمة | من يملكه |
|---|---|---|
| نقاط اليوم | حضور ١٠ · ثوب ٢ · الدرس ٥ · م.ص ٢ · م.ك ٥ | يحسبها النظام من تسجيل المعلم |
| الاختبارات | فضي ٥٠/١٠٠ · ذهبي ١٠٠/٢٠٠ · الجمعية ٢٠٠ | المشرف عند تسجيل النتيجة |
| أكواد الباركود | بحسب الدفعة | المشرف يُصدرها، والطالب يشحنها |
| الخصم | — | **المشرف وحده** |

**لا حذف لحركة أبدًا.** التصحيح بحركة معاكسة مسجَّلة (`CORRECTION`).

**وطلاب التلقين خارج النقاط كلّها** — ٤٢ طالبًا. والمنع **في الخادم** عند بوّابة واحدة
(`pointsScope`) تقرأ مساره من القاعدة لا مما أرسله، فترّد ٤٠٣. إخفاء التبويب وحده لم يكن
منعًا: كان الطلب المباشر يمرّ.

**والذرّية مثبتة:** الشراء والاستبدال يستعملان تحديثًا مشروطًا (`updateMany ... WHERE`)
لا قراءةً ثم كتابة — فطالبان يضغطان «شراء» على آخر قطعة في اللحظة نفسها يُنتجان طلبًا
واحدًا لا اثنين.

---

## ٦ · الخطط والمستويات

- **المنهج** ٣٥٧٢ صفًّا: لكل مسار ومستوى ٢٤ مقرّرًا، لكل مقرّر ثلاثة أسطر.
- **المقرّر ليس تاريخًا**: الطالب الذي غاب أسبوعًا يبقى على مقرّره حتى يسمّعه.
- **الانتقال بالدرس وحده**، وبترتيب التسجيل لا بترتيب التقويم.
- **المقرّران ١٢ و٢٤** اختباران: يقف عندهما حتى تُسجَّل نتيجته.
- **الإصدار والطباعة عند المشرف**؛ والمعلم يعرض ويطبع ويرسل لوليّ الأمر.
- **التعديل للخطة كلّها** لكل من يأخذ ذلك المستوى — لا لطالب بعينه.

---

## ٧ · السرعة — وما فُعل فيها

كان `GET /api/state` — وهو ما تطلبه كل شاشة مشرف عند فتحها — **يتجاوز التسعين ثانية**.
قياسًا على القاعدة الحيّة، جدولًا جدولًا:

| الجدول | الصفوف | الحجم | الزمن |
|---|---|---|---|
| **المنهج** | ٣٥٧٢ | **٥٢٠ ك.ب** | **٧٣ ثانية** |
| الاختبارات | ٤١٢ | ١٤٩ ك.ب | ٤٫٣ ثانية |
| الطلاب | ١٢٠ | ٥٩ ك.ب | ٨ ثوانٍ |
| الخطط | ١٦٨ | ٤٧ ك.ب | ٩٫٢ ثانية |

**المنهج وحده ثلثا الحمل وجُلّ الانتظار** — لجدول لا تقرؤه إلا شاشتا الخطط والاختبارات.

**ما فُعل:**

1. **أُخرج المنهج** إلى `GET /api/curriculum` — يُطلب عند الحاجة، وبـETag.
   المتصفّح الذي يحمله يأخذ **304 بلا جسم في ثلث ثانية**.
2. **الضغط.** Next لا يضغط ما تُرجعه المسارات — كانت نصف ميغابايت من نصوص متكرّرة
   تعبر الشبكة خامًا. `lib/compress.ts` يضغطها gzip حين يقبلها الطالب.

| | قبل | بعد |
|---|---|---|
| `/api/state` | ٢٩٦ ك.ب · انقطاع عند ٩٠s | **٦٣٫٧ ك.ب · ٣٫٩s** |
| `/api/curriculum` | ٥٣٢ ك.ب · ٧٤s | **٢٨٫٨ ك.ب · ٢٫٨s** ثم 304 |

> **وبصمة المنهج واحدة في موضعين:** الـETag هنا، وقرار «هل تغيّر المنهج؟» في الحفظ —
> تُحسب في Postgres وتعود صفًّا واحدًا (١٫٢ ثانية بدل ١٠–٦٠). والطيّ على **بصمة كل صفّ**
> لا على نصّه، لأن النصّ عربي وترتيب Postgres للعربية يخالف ترتيب JavaScript،
> والبصمات سداسية عشرية فيتّفقان.

**ومهلة المعاملة ١٢٠ ثانية** لا ٣٠: الثلاثون اختيرت يوم كان الحفظ صغيرًا والقاعدة قريبة.

---

## ٨ · الأمن والخصوصية

- **المستودع عامّ** — باختيار العميل. فلا تدخله بيانات أحد.
- **`*.xlsx` متجاهَل** في `.gitignore`، وملفا الحسابات يُكتبان **خارج المستودع** بصلاحية `600`.
- **لا هويّة ولا جوّال ولا اسم طالب** في أيّ ملف مُودَع.
- **`bcrypt` بكلفة ١٢** لكل كلمات المرور.
- **٥ محاولات خاطئة ← قفل ١٠ دقائق** (الطالب والمعلم).
- **رسالة الخطأ لا تكشف أيّ الحقلين كان خطأ**، وتُجزَّأ كلمة وهمية حين لا يوجد المستخدم
  فلا يُفرَّق بالتوقيت.
- **التصفير يطلب رمزًا** يُتحقَّق منه **في الخادم** (`RESET_PHRASE`).
- **لا حذف لطالب أبدًا** — المنقطع يُعلَّم ويبقى ملفه.

> **وحساب `admin/12345` ما زال قائمًا وكلمته معروفة في المستودع العامّ.**
> يُوقَف بعد أن يدخل المشرفون الأربعة ويغيّروا كلماتهم.

---

## ٩ · الجداول (٢٧)

**الأساس:** `admin_users` · `halaqat` · `students` · `halaqa_transfers` · `settings` ·
`audit_log` · `import_runs`

**النقاط والمتجر:** `point_txns` · `point_code_batches` · `point_codes` · `gifts` · `orders`

**الاختبارات:** `exams` · `exam_questions` · `exam_bookings` · `tajweed_topics`

**المنهج والخطط:** `curriculum_days` · `student_plans`

**الحسابات:** `student_credentials` · `teachers`

**بوابة المعلم:** `day_entries` · `recitation_lines` · `day_entry_revisions` ·
`student_progress` · `admin_messages` · `student_alert_reads` · `teacher_alert_reads`

**١٤ ترحيلًا**، آخرها تسعة أُجريت ١٨ سبتمبر لبوابة المعلم.

---

## ١٠ · أخطاء وقعت فعلًا — ولماذا هي هنا

كل واحد منها كلّف وقتًا أو بيانات، وكل حارس في النظام جاء من واحد منها.

| ما وقع | لماذا | ما يمنعه اليوم |
|---|---|---|
| **حفظ فارغ محا القاعدة — مرّتين** | متصفّح لم يكتمل تحميله أرسل قوائم فارغة | حارس 409 |
| **طالب مضاف لا يصل القاعدة** | المزامنة ترسل `{id, dedupeKey}` فقط، والحفظ لا يكتب طالبًا | السجلّ يُرسل كاملًا ويُكتب |
| **حلقة مضافة أو محذوفة لا تصل** | الحلقات ليست في قائمة المزامنة أصلًا | أُضيفت، والمحذوفة تُحذف وطلابها «بلا حلقة» |
| **حذف هديّة اشتراها أحد يُسقط الحفظ كلّه** | `gift_id` مطلوب ومربوط، فالمعاملة تنهار | الرابط اختياري يُقطع ولا يجرّ |
| **«مسح تسجيل اليوم» يمنع كل حفظ بعده** | حركة التصحيح فقدت `ref_type` فعادت في القائمة | ترحيل يُصلح القائم، والشيفرة لا تكتبها كذلك |
| **الحضور يُقرأ نعم/لا** | العمود **عدد أيام** لا حالة — فطالب حضر ٣ من ٣ قُرئ «غائب» | `attendedDays` عددًا |
| **مستوى يقفز ٢٣ ← ٤٠** | `issuePlan` تُنادى أثناء الرسم فتكتب | `planFor()` تقرأ، والترقية عند الطباعة |
| **٧٩ من ٨٤ ماسيًّا بلا أجزاء** | النموذج لا يقترح لمستوى في منتصف جزء | `ajzaExact` و`ajzaForExam` |
| **١٤ اختبارًا لا تعمل بصمت** | ملف الاختبار ينادي دالة غير موجودة فلا يُحلَّل | — |
| **زرّ الحذف لا يعمل** | حالتان: الأزرار على واحدة والنافذة على أخرى | حالة واحدة |
| **التحية «أبا عبدالله» للجميع** | نصّ ثابت، صحيح لحساب واحد | تُقرأ من الجلسة |
| **الحفظ يموت بالمهلة** | المنهج يُهدم ويُبنى، والطلاب يُكتبون بلا شرط | لا يُكتب إلا ما تغيّر |

---

## ١١ · ما ينتظر

- **النشر على CranL** — عدّة إصلاحات مدفوعة والبناء الحيّ متأخّر عنها.
- **إيقاف `admin/12345`** بعد دخول المشرفين.
- **الرابط إلى CranL بطيء جدًّا** — قراءة واحدة بلغت ٦٠ ثانية في القياس.
  لا شيء في الشيفرة يعالج شبكة بهذا البطء، وهو يستحقّ نظرًا مستقلًّا.
- **تسعة طلاب هويّاتهم أقصر من عشرة أرقام** — أقصرها أربعة. يدخلون بلا مشكلة،
  لكنها كلمة مرور ضعيفة.
- **أسئلة معلّقة على العميل:** أيام انعقاد الحلقات في الأسبوع وإجازاتها (ولا يكتمل
  حساب الغياب قبلها) · مضاعفة نقاط الذهبي أتشمل الحضور · جوّال وليّ الأمر أيُعرض للمعلم.

---

## ١٢ · تشغيله

```bash
npm install
npx prisma generate
npx prisma migrate deploy     # يحتاج DATABASE_URL في البيئة
npm run dev                   # أو: npm run build && npm start
npx vitest run                # ٣٠٢ اختبارًا
```

`.env.local` يحمل `DATABASE_URL` و`SESSION_SECRET`، وصلاحيته `600`، وهو مُتجاهَل في git.
